# 前端开发环境 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 先复制 package.json 和依赖安装
COPY package*.json ./
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
